import {
  createSolvedCubeState,
  applyMoveToState,
  solveCube,
  cubeStateToFaceGrid,
  validateCubeState
} from '../src/solver/cubeState.js';

// Wait, we need to test using the actual built / compiled functions or with tsx
